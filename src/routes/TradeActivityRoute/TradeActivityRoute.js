import React from 'react'
import TradeActivity from '../../containers/TradeActivity'

const TradeActivityRoute = ({ title }) => props => (
  <TradeActivity {...props} title={title} />
)

export default TradeActivityRoute
